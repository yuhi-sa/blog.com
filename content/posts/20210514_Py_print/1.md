---
title: "Pythonでprintの上書きをする方法"
date: 2021-04-16T10:00:23+09:00
draft: false
images: ["https://yuhi-sa.github.io/ogp.jpeg"]
description: "Pythonでprint出力を上書きする方法を解説。キャリッジリターンによる一行上書きと、ANSIエスケープシーケンスを使った複数行の上書き方法を紹介します。"
tags: ["Python"]
keywords:
  [
    "Python",
    "print",
    "上書き",
    "キャリッジリターン",
    "ANSIエスケープシーケンス",
    "コンソール出力",
  ]
---

<!--more-->

## 一行上書きする場合

一行を上書きする場合、`
`（キャリッジリターン）を使用します。これはカーソルを行の先頭に戻す制御文字です。

```python
print("\r"+"一行を上書きします！",end="")
```

`end=""`を使用して改行を行わないようにします。

## 複数行を上書きする場合

複数行を上書きするような表示を行う場合、特殊なエスケープシーケンス`\033[nA`（カーソルをn行上に移動）などを利用します。

```python
print("最初の行")
print("二番目の行")
# カーソルを1行上に移動し、上書き
print("\033[1A新しい二番目の行")
```

## モダンなPython出力ライブラリ

`print`の上書き以外にも、リッチな出力を実現するライブラリがあります。

### richライブラリ

[rich](https://github.com/Textualize/rich)はターミナルに色付きテキスト、テーブル、プログレスバーなどを表示できるライブラリです。

```python
from rich.console import Console
from rich.table import Table

console = Console()

# プログレス表示
from rich.progress import track
import time

for i in track(range(100), description="Processing..."):
    time.sleep(0.01)

# ステータス表示（スピナー付き）
with console.status("Computing..."):
    time.sleep(2)
    console.print("[bold green]Done![/bold green]")
```

### tqdmによるプログレスバー

[tqdm]({{< ref "/posts/20260223_progress_bar/1.md" >}})は簡潔な記述でプログレスバーを表示できます。上書き表示の代替として広く使われています。

```python
from tqdm import tqdm
import time

for i in tqdm(range(100)):
    time.sleep(0.01)
```

用途に応じて、シンプルな上書き表示には本記事の方法を、リッチな表示には`rich`や`tqdm`の利用を検討してください。

## 関連記事

- [Pythonでプログレスバーを自作する（tqdmなし）]({{< ref "/posts/20260223_progress_bar/1.md" >}}) - この記事で紹介したキャリッジリターンやANSIエスケープシーケンスを活用して、プログレスバーを自作する方法を解説しています。
